import Foundation

 
 
 
 
 
 
 
 
 
 
enum CoreRestartPolicy {
     
     
     
     
    static func isOffered(isConnected: Bool) -> Bool {
        isConnected
    }

    static let confirmation =
        "Every open connection is dropped and the tunnel comes back up with the current profile."
}
